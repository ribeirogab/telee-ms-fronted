// const GeneralReport = require('../models/GeneralReport')
// const EditorReport = require('../models/EditorReport')

module.exports = {
  generalReports (day, month, fullYear) {
    console.log(day, month, fullYear)
  },

  editorReports (day, month, fullYear) {
    console.log(day, month, fullYear)
  }
}
